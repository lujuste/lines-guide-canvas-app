export interface Theme {
  theme: {
    name: string;
    colors: {
      primary: string;
      black: string;
      background: string;
      border: string;
      gray900: string;
      gradientBody: string;
      gradientBody2: string;
      gray300: string;
      gray500: string;
      editorBackground: string;
      red?: string;
    };
  };
}
